apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-app-deployment
  labels:
    app: python-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-app
  template:
    metadata:
      labels:
        app: python-app
    spec:
      containers:
      - name: python-app
        image: s4sachin/pythonapp:db7348ac0800cb7c70357005f27f7e4d2a03852e
        ports:
        - containerPort: 5000

---
apiVersion: v1
kind: Service
metadata:
  name: pythonapp-service
spec:
  selector:
    app: python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pythonapp-ingress
  namespace: default
  annotations:
    spec.ingressClassName: traefik 
spec:
  rules:
    - host: pythonapp.myk3shomelab.xyz 
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pythonapp-service  
                port:
                  number: 80

